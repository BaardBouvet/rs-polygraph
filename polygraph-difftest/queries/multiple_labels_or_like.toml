name = "multiple_labels_or_like"
description = "Match nodes with either of two labels using [:A|B] notation."
spec_ref = "openCypher 9 §2.1 Patterns — label alternation"

cypher = """
MATCH (n:Cat)-[:OR_LIKE]->(m)
RETURN 1 AS dummy
LIMIT 0
"""

[fixture]
nodes = []
edges = []

[expected]
columns = ["dummy"]
rows = []
order = "bag"
