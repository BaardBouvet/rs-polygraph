name = "range_unwind_step"
description = "UNWIND range(start, end, step) with explicit step."
spec_ref = "openCypher 9 §6.3.3 — range() with step"

cypher = """
UNWIND range(0, 10, 3) AS x
RETURN x
ORDER BY x
"""

[fixture]
nodes = []
edges = []

[expected]
columns = ["x"]
rows = [[0], [3], [6], [9]]
order = "ordered"
