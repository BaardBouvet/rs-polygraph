/// ISO GQL AST node types — stub for Phase 5.
///
/// This module will be populated in Phase 5 when GQL support is added.
/// Declaring it here keeps the module tree stable across phases.

/// Placeholder for the top-level GQL query AST node.
#[derive(Debug, Clone, PartialEq)]
pub struct GqlQuery {
    _private: (),
}
